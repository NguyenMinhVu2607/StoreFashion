<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StoreFashion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StoreFashion" parent="Base.Theme.StoreFashion" />
        <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView">
            <!-- Removes label visibility -->
            <item name="labelVisibilityMode">labeled</item>
            <!-- Set the icon and text color to the same, so the color change is not visible -->
            <item name="itemIconTint">@color/colorPrimary</item>
            <item name="itemTextColor">@color/colorPrimary</item>
        </style>
</resources>